# 🚨 STRICT FINAL REBUILD & AUDIT - MT5 BOT vs bobot2.py

🎯 Tujuan:
1. Pastikan bot trading MT5 ini 100% identik secara visual, fungsional, dan arsitektur dengan bobot2.py.
2. Perbaiki semua fitur yang hilang, terutama pada GUI & pre-start settings.
3. Integrasikan semua parameter user ke logika strategi sebelum bot start.
4. Buat file .bat untuk menjalankan bot di Windows.
5. Lakukan crosscheck akhir dan dokumentasi hasil audit.

---

## 1️⃣ AUDIT & PERBAIKAN WAJIB

### A. PRE-START SETTINGS (❌ MISSING)
- Tambahkan tab konfigurasi strategi yang memungkinkan user mengatur:
  - TP (pips, price, %, currency)
  - SL (pips, price, %, currency)
  - Lot size manual atau auto-lot
  - Strategi awal (HFT, Scalping, Intraday, Arbitrage)
  - News filter ON/OFF
- Pastikan semua ini diatur sebelum bot dijalankan.

### B. STRATEGY PARAMETER STORAGE (❌ MISSING)
- Buat `self.strategy_params` dictionary seperti bobot2.py.
- Simpan semua pengaturan user di dictionary ini untuk digunakan saat eksekusi strategi.

### C. GUI METHODS UNTUK PARAMETER RETRIEVAL (❌ MISSING)
Implementasikan method berikut di GUI class:
```python
get_current_lot()
get_current_tp()
get_current_sl()
get_current_tp_unit()
get_current_sl_unit()
Semua method harus mengembalikan nilai sesuai input user di GUI.

Nilai harus terhubung ke self.strategy_params.

D. MULTI-UNIT TP/SL (⚠️ PARTIAL)
Pastikan mendukung:

Pips

Price

Percentage (%)

Currency (USD, EUR, GBP, CAD, AUD, JPY, CHF, NZD)

Integrasikan langsung ke perhitungan strategi saat bot berjalan.

E. STRATEGY TAB LAYOUT (❌ WRONG)
Ubah layout menjadi 4 panel strategi dalam 2x2 grid.

Setiap panel punya pengaturan TP/SL/Lot sendiri sesuai bobot2.py.

2️⃣ BACKEND INTEGRATION
Hubungkan semua nilai dari GUI → self.strategy_params → strategi trading.

Pastikan perubahan setting di GUI langsung mempengaruhi strategi sebelum eksekusi order.

Tambahkan logging saat parameter diubah.

3️⃣ .BAT FILE CREATION
Buat file start_bot.bat:

bash
Copy
Edit
@echo off
cd /d "%~dp0"
python bobot2.py
pause
Pastikan bisa dijalankan di Windows dengan double-click.

4️⃣ FINAL CROSSCHECK
Buat tabel perbandingan bobot2.py vs bot saat ini (✅ match, ⚠️ partial, ❌ missing).

Sertakan screenshot GUI final.

Laporan error fix.

Hasil uji pre-start settings.

Verifikasi semua strategi berjalan sesuai parameter GUI.

TARGET HASIL
✅ GUI identik dengan bobot2.py
✅ Semua fitur pre-start berfungsi penuh
✅ Strategi menggunakan parameter GUI
✅ Multi-unit TP/SL bekerja sempurna
✅ File .bat bisa start bot di Windows
✅ Tidak ada error/bug
✅ Siap live trading